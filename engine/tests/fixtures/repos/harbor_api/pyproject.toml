[project]
name = "harbor-api"
version = "0.1.0"
description = "Static retrieval fixture for DeCodifier"
requires-python = ">=3.11"

[tool.harbor]
service = "harbor-api"
domain = "project-collaboration"
