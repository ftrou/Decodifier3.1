# Harbor API

Static fixture repo for retrieval validation.

Focus areas:

- Python-only service layout
- auth and session flows with direct method calls
- caller-site permission checks
- realistic naming noise in non-critical modules
