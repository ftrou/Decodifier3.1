# Introduction

DeCodifier is a local-first engine that exposes deterministic tools for LLMs to
inspect and modify real codebases. It keeps projects on disk, avoids vendor
lock-in, and focuses on safe file operations.
